Again log in as bandit26 with the ssh key. Make sure you resize your terminal before connecting to force more to page through the file. Then type `v` to switch to vim and enter the following command:
```
:set shell=/bin/bash
:! ls -lsa
:!/home/bandit26/bandit27-do cat /etc/bandit_pass/bandit27
```

Password for level 27 is `YnQpBuifNMas1hcUFk70ZmqkhUU2EuaS`.
