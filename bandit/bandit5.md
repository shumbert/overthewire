There are a bunch of files in the `inhere` directory, however instructions say:
> human-readable
  1033 bytes in size
  not executable

You can use the following command to find it:
```
find . -type f -size 1033c
```
