So you have to run two commands, first run netcat:
```
nc -lnvp 9999
```

Next run this command:
```
./suconnect 9999
```

From the netcat window send the password from the current level, suconnect answers with the next level password.

Password for level 21 is `NvEJF7oVjkddltPSrdKEFOllh9V1IBcq`.
