Clone the repo using the command:
```
GIT_SSH_COMMAND='ssh -o PubkeyAuthentication=false -p 2220' git clone ssh://bandit27-git@localhost/home/bandit27-git/repo
```

Password is in the repo README file.

Password for the next level is `AVanL161y9rsbcJIsFHuw35rjaOM19nR`.
