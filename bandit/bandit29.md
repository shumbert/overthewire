Clone the repo using the command:
```
GIT_SSH_COMMAND='ssh -p 2220' git clone ssh://bandit29-git@localhost/home/bandit29-git/repo
```

List remote branches then switch to the dev one:
```
GIT_SSH_COMMAND='ssh -p 2220' git ls-remote
git checkout dev
```

Password is in README.md.

Password for the next level is `xbhV3HpNGlTIdnjUrdAlPzc2L6y9EOnS`.
